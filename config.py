# config file contains parameters that could possibly be chaning during each run,
# its job is to replace the command line input method.

# configs for modules
lpips_root = None

# configs for envs
pad_width = None
n_nbs = 5
invalid_depth_to_inf = True
bwd_depth_thresh = 0.1
verbose = 1
vis = False